// Test con operatore :: tipo errato - int :: int invece di int :: list
def main() {
    x = 5
    y = 10
    result = x :: y  // Dovrebbe generare errore: y deve essere una lista
    return head(result)
}
