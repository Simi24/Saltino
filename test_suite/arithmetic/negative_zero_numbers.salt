// Test con numeri negativi e zero
def main() {
    zero = 0
    negative = -42
    positive = 42
    result1 = zero + negative
    result2 = negative + positive
    result3 = zero * 100
    result4 = negative / negative
    return result1 + result2 + result3 + result4
}
