// Test di chiamate annidate di funzioni
def main() {
    return compute(add(2, 3), multiply(4, subtract(10, 5)))
}

def compute(a, b) {
    return add(a, b) * 2
}

def add(x, y) {
    return x + y
}

def multiply(x, y) {
    return x * y
}

def subtract(x, y) {
    return x - y
}
