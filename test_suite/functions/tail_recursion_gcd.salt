// Test di GCD (Greatest Common Divisor) con tail recursion
def main() {
    return gcd_tail(48, 18)
}

def gcd_tail(a, b) {
    if (b == 0) {
        return a
    } else {
        return gcd_tail(b, a % b)
    }
}
