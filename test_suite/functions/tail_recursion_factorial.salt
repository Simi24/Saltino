// Test di factorial con tail recursion
def main() {
    return factorial_tail(5, 1)
}

def factorial_tail(n, acc) {
    if (n <= 1) {
        return acc
    } else {
        return factorial_tail(n - 1, n * acc)
    }
}
