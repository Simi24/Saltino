// Test di power/exponentiation con tail recursion
def main() {
    return power_tail(2, 10, 1)
}

def power_tail(base, exp, acc) {
    if (exp == 0) {
        return acc
    } else {
        return power_tail(base, exp - 1, acc * base)
    }
}
